<Window x:Class="IshbulatovGlaza1920.PrioritetWindow1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IshbulatovGlaza1920"
        mc:Ignorable="d"
        Title="PrioritetWindow1" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Message}" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Новый приоритет:" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="PriorityTextBox" FontSize="18" Height="35"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>
